export default function MarketingPage() {
  return (
    <div className="flex flex-col gap-6">
      <section className="px-0">
        <h2 className="text-secondary mb-2!">Marketing</h2>
        <p className="small">
          This page uses the shared layout (sidebar + dashboard header).
        </p>
      </section>
    </div>
  );
}

